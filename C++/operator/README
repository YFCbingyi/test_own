C++函数对象
函数对象：定义了调用操作符()的类对象。当使用该对象调用此操作符的时候，其表现形式如同普通函数调用一样，因此取名叫函数对象。

1、最简单举例 01
------------------------------------------------
与普通函数相比，函数对象比函数更加灵活，函数对象的优势
- 函数对象可以有自己的状态，我们可以在类中定义状态变量，这样一个函数对象在多次的调用中可以共享这个状态。
- 
函数对象有自己特有的类型。我们可以传递相应的类型作为参数来实例化相应的模板，比如带参数的函数形参
2、典型例子 02

